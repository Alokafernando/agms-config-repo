server:
  port: 8081

spring:
  application:
    name: zone-service

  datasource:
    url: jdbc:mysql://localhost:3306/agms_sensor_db?createDatabaseIfNotExist=true
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
